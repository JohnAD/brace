#!/lang/b
use b

Main()
	if cstr_eq(program, "split_html")
		html_split()
	 eif cstr_eq(program, "join_html")
		html_join()

# idea - functions that have access to env of caller "automatically" without
# having to pass in params, create automatically for each macro YYY!
