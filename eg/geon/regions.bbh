export vector
export poly

using namespace std

enum region_type
	LAND,
	LAKE,

struct region
	int id
	region_type type
	poly_angle3 points_a
	poly_vec3 points_v

load_and_convert_regions(const char *datafile)

extern vector<region> regions

