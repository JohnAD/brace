extern main  int argc  char *argv[]
	gr_init 400 300
	if argc < 2
		error "syntax: %s depth [colour]" argv[0]
	if argc > 2
		col argv[2]
	clear
	int depth
extern tree  num x  num y  num a  num r  int depth
	if depth

